=breadcrumb_tag do
  %li.active
    = link_to t('breadcrumb.users'), users_path
  %li.active
    = t('headers.users.edit') + " " + @user.email

.page-header
  %h1= t('headers.users.edit') + " " + @user.email

= render 'form'
