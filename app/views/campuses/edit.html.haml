=breadcrumb_tag do
  %li.active
    = link_to t('breadcrumb.campuses'), campuses_path
  %li.active
    = t('headers.campuses.edit', campus: @campus.name)

.page-header
  -# %h1= t('headers.campuses.edit') + " «" + @campus.name + "»"
  %h1= t('headers.campuses.edit', campus: @campus.name)

= render 'form'
